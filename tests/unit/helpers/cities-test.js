import { cities } from '../../../helpers/cities';
import { module, test } from 'qunit';

module('Unit | Helper | cities');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = cities(42);
  assert.ok(result);
});
